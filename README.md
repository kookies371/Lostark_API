# 로스트아크 캐릭터 정보 조회 웹서비스

로스트아크 게임에서 캐릭터 이름을 입력하면 장비 정보를 웹에서 간편하게 조회할 수 있는 서비스입니다.

## 🚀 빠른 시작

### 설치

```bash
pip install -r requirements.txt
```

JWT 토큰을 `.env` 파일에 설정하세요:
```
LOSTARK_JWT=your_token_here
```

### 실행

**백엔드 (FastAPI):**
```bash
uvicorn app.main:app --reload
```

**프론트엔드 (Streamlit):**
```bash
streamlit run ui/app.py
```

## 📖 사용 방법

1. Streamlit UI (http://localhost:8501)에서 캐릭터 이름을 입력
2. 검색 버튼 클릭
3. 캐릭터의 장비 정보를 확인

## 📚 개발 문서

자세한 개발 정보는 [CLAUDE.md](CLAUDE.md)를 참고하세요.

## 📄 라이선스

학습 목적으로 작성되었습니다.
